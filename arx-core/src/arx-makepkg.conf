# ARX makepkg safety/performance defaults.
# Included as a reference configuration; ARX never disables package verification.
# Parallel compilation is delegated to makepkg's MAKEFLAGS and the user's toolchain.
DLAGENTS=()
# Keep source/build caches outside the final package install path.
